# 주보 엔진 — 다음 세션 인수인계 (2026-07-21 기준)

## 지금 상태 (전부 라이브)
- **kzion.net/bt** 배포 완료. 로그인(목사님/관리자 PIN, PPT와 공유) → 주보 편집 → 미리보기·인쇄(트라이폴드 legal 6패널).
- **PPT(ppt/)는 무변경.** 주보는 별도 폴더(bt/) + 별도 Edge Function(bt).
- 브랜치: `main`이 최신. 작업은 `bulletin-engine`에서 하고 `--no-ff` 머지 후 push.
- 교인 명단 등 개인정보는 Supabase에만. repo엔 실명 0건(익명화 규칙 유지).

## 완료된 기능
- 표지(마스트헤드: 로고 없음, 리본/교회명/배지/성구, 살구색 톤), 권/호·날짜 자동
- 예배순서: 14줄 **인라인 편집**(PPT·자동값 기본, 고치면 주보 override, 비우면 복귀). 설교 볼드.
  대표기도 직분 자동(정승진 장로/이성훈 집사). 특송=성가대 PPT 자동("곡명 · 성가대"/"곡명 · OOO").
- 교회소식: 연간 행사표 기반 자동 안내(오늘은…/다음 주일은…/이번 주 …) + 수동, 자동도 편집 가능
- 토요새벽예배: 날짜 자동(그 주 토요일)·설교 본문·담당자(기본 담임목사)
- 사랑의 나눔(4주, 이번 주만 입력), 지난주 헌금(타이핑, 두 글자 이름 자동 정렬 "김 정", 이름 사이 넓게)
- 명단 관리 + 로테이션 수동 개입 3종(이번주만/당기기/끼워넣기), 인쇄 스냅샷 잠금
- 인쇄 게이트(필수 빈칸 경고, 그래도 인쇄 가능), 세로 접는 점선 제거
- (2026-07-22) 예배순서 순서명 **양끝맞춤**(`text-justify:inter-character` — Chrome·Edge OK, Safari는 미지원이라
  좌측정렬로 폴백), **주보 전체 본문 폰트 ~+0.5pt 상향**, 교회소식 **자동 안내 본문 일반 굵기**(제목만 볼드),
  편집화면 자동 안내 칸을 테두리 있는 입력 필드로(수정 가능함이 보이게)
- (2026-07-22) **5종 3차**: (1)섬기는이들 날짜칸 74px+padding-right 11px(양옆 여백), 기도칸 20%로 축소.
  (2)행사계획 기본 자동 4개(`.slice(0,4)`)+수동 추가/삭제: config `bulletinEvents(S)`, bulletin.data `eventsHidden`/
  `eventsAdded`, 편집 renderEventsCard 재작성(자동 ✕숨김/되살리기 + 수동 날짜·내용 입력). (3)설교노트 제목 수정
  (bulletin.data `notePanels[idx].title`, 기본 "설교노트"). (4)설교노트 이미지+본문(notePanels[idx].image_data·text,
  둘 다 비면 손글씨 빈 줄). print `panelSermonNote(S,idx)`, 편집 `renderNotePanelCard(S,idx,label)` 2장. (5)예배순서
  순서명↔내용 gap 14→21px. bt.css v18→19, print.css v19→20, config v15→16, print.js v17→18, app v16→17.
- (2026-07-22) **9종 보완 2차**: (1)표 헤더행(사랑나눔·섬기는이들) 배경 살구색 `#FCF6EE`(세로축과 동일, `.p-grid th`).
  (2)표 날짜 `fmtMDKorean`="M월 D일"(사랑나눔·행사·섬기는이들). (3)사랑나눔 날짜 가운데(유지). (4)행사·섬기는이들
  날짜 오른쪽(`.pe-date`·`.cover-serve .p-grid-rh` text-align:right). (5)**표지 예배순서 간격**: 표지가 물리적으로
  꽉 참(14줄+마스트헤드+섬기는이들표=8.5in) → 마스트헤드·안내문·섬기는이들표 압축해 확보(~1.1배가 실질 최대,
  2~3배 불가). 15줄(성찬식)도 오버플로 없음 확인. **더 벌리려면 섬기는이들표 이동/순서 폰트 축소 필요**. (6)토요새벽
  p-sat-k 양끝맞춤. (7)토요새벽 설교=본문·담당 둘 다 표시(sat.sermon+preacher, 원래부터 편집 가능 — 설교 본문칸에
  "고린도후서 강해" 입력하면 나옴). (8)**폰트 분리**: 편집 UI=Pretendard 복원(bt.css body+index.html 링크),
  인쇄물만 나눔스퀘어네오(`.sheet-page` font-family). (9)**이 주 되돌리기**(week-reset 버튼): 클라 bulletin.data
  통째 비움(auto 복귀) + 서버 `resetRotations`(이 주 is_manual·미locked rotation_assignments 삭제, 앵커는 보존).
  **★ resetRotations는 bt Edge Function 재배포 필요**(미배포 시 로테이션은 안 지워지고 주보 입력만 초기화됨).
- (2026-07-22) **8종 보완**: (1)※찬송·축도 = `[※][co-lab]` 구조로 ※ 제외하고 라벨만 양끝맞춤(print co-k flex,
  app order-label flex). (2)편집 빈 입력칸 강조(order-input `:placeholder-shown` 경고색 + "입력" 골드 볼드,
  전역 placeholder 진하게). (3)**성찬식 자동삽입**=`communionThisWeek`면 buildOrderRows가 설교 뒤 삽입,
  이번 주만 빼기=`bd().hideCommunion`(notice 버튼 토글). (4)행 간격: **편집화면만** order-row 12px로 확대,
  **인쇄 표지는 2.4px 유지**(space-between이 이미 패널 채움 + 성찬식 15줄이면 늘리면 안내문과 겹침=오버플로). (5)글자 전부
  #111 검정(--sky-ink·권호·성구·주소·표헤더 등), **예외 3=리본 영문·표지 날짜·뱃지(흰글씨/오렌지알약)**. (6)사랑의나눔
  `.p-love` table-layout:fixed 첫칼럼 `--info-c1:64px`+주차 4칸 균등. (7)토요새벽 p-sat-k·행사 pe-date도 64px 통일
  (합심기도 한 줄). (8)예배찬양 악보 인쇄 시 패널 꽉 차게(max-height 3.4in 상한 제거→100%), 업로드 시 밝은 배경
  흰색 정규화(resizeImageToDataURL luminance>200→흰색, 음표 보존), 편집 미리보기 박스 흰색. **크림 악보는 재업로드해야 흰색 적용.**
- (2026-07-22) **소제목 바 배경 연한 하늘색**(`--sky` 토큰, 설교노트·예배찬양·사랑의나눔·토요새벽·표 헤더 등.
  교회표어 p-motto는 살구색 유지), **표지 "시애틀 시온장로교회" 검정**, **폰트 나눔스퀘어네오 통일**
  (bt.css @font-face noonnu jsdelivr, 리본 영문만 Georgia 유지), **표지 뱃지+성구 한 줄**(cover-tagline),
  **섬기는 사람들 원본 격자표**(serveTable, 라벨로 배치·핵심라벨 없으면 2열 폴백), **예배찬양 악보 업로드**
  (renderPraiseCard — 서버 없이 클라 리사이즈 1500px→data URL을 bulletin.praise_panel.image_data에 저장, 글 모드 겸용)

## 아키텍처 요점 (새 세션이 알아야 할 것)
- 코드: `bt/index.html`, `bt/js/{config,api,app,print}.js`, `bt/css/{bt,print}.css`
- **config.js = 공용 로직**: `buildOrderRows(S)`(예배순서), `autoNewsItems(S)`(교회소식 자동),
  `choirLine`, `orderTitleLabel`(직분), `saturdayOf`, `padTwoCharNames`/`nameSpans`(이름 정렬).
  app(편집)와 print(인쇄)가 **같은 함수**를 써서 "편집=인쇄" 일치.
- 서버: `supabase/functions/bt/index.ts` (563줄). getBulletin이 pastor/serve/love/events/meta/members/**choirSongs** 반환.
- 저장: `bulletin_inputs.data`(주보 전용, orderOverrides·news·autoNewsEdits·saturday·offering 등),
  로테이션은 `rotation_anchors`(앵커=이름) + `rotation_assignments`(오버라이드).
- 캐시 버스팅: bt 수정 시 `bt/index.html`의 `?v=NN` 올릴 것. 현재 css=v14, js=v13.
- 로컬 미리보기: `/tmp/kzion-preview`에 rsync 후 python http.server 8848 (구글드라이브 경로는 직접 서빙 불가).

## ⚠️ 아직 안 된 것 / 주의
1. **bt 함수 마지막 재배포는 사용자가 방금 성공**(choirSongs 포함). 앞으로 서버 또 고치면
   대시보드 붙여넣기 시 **파일이 길어(563줄) 중간에 잘리는 사고** 주의 — 붙여넣고 맨 끝이 `});`인지 확인.
2. **입력 칸 자동 늘어남 + 넘침(overflow) 감지·자동 축소**(컨셉 락 §6) 미구현. 이름 많은 주 대비.
   → 실제 PDF 뽑아보고 몇 명까지 한 장에 들어가는지 보고 기준 잡기로 함.
3. **PPT 페이지에 「주보」 탭 통합**(사용자 최종 요구: 한 페이지에서 다 하기) 미구현. 인쇄 검증 끝난 뒤 마지막에.
4. 실제 프린터 여백(가장자리 3~5mm 잘림) 실물 1장 확인 필요.

## 다음 세션에 붙여넣을 첫 메시지 (예시)
> kzion-site 주보 엔진(kzion.net/bt) 이어서 작업하려고 해.
> `docs/주보엔진_인수인계_다음세션.md`와 `docs/주보엔진_컨셉락_v1.md`(B1~B11 확정사항) 먼저 읽어줘.
> 원칙: PPT(ppt/)는 절대 문제 생기면 안 되고, 교인 실명은 repo에 넣지 말 것(Supabase만).
> 작업은 bulletin-engine 브랜치 → main 머지 → push, bt 수정 시 index.html ?v= 올리기.
> 오늘 할 것: [아래 중 택1] (a) 넘침 감지·자동축소 (b) PPT에 주보 탭 통합 (c) 디자인 추가 보완
