-- ============================================================
-- 특송 개편: 곡마다 '이름/팀' 저장 칼럼
-- Supabase 대시보드 → SQL Editor 에 붙여넣고 [Run]
-- 안전: IF NOT EXISTS 라 여러 번 실행해도 문제 없음
-- ============================================================

-- 특송 이름/팀 (예: '남성중창단', '김하늘 집사').
--   성가대 곡은 빈 값 → 자막 크레딧은 항상 '시온 성가대'
--   특송 곡은 이 값 → 자막 크레딧 '특송 · <이름/팀>'
-- (곡 제목은 기존 name 칼럼에 저장 — 성가대·특송 공통)
alter table songs add column if not exists song_performer text not null default '';
