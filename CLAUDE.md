# CLAUDE.md — kzion_site 주일예배 PPT 자동 생성 허브

이 문서는 이 프로젝트의 단일 진실 소스(컨셉 락)다. 아래 지침 v3.2의 모든 확정 사항을 따르고, 결정이 바뀌면 이 파일을 즉시 갱신한다.

## Claude Code 작업 규칙

### 작업 방식
1. **계획 먼저, 실행은 승인 후**: 기능 단위 작업 전에 결정이 필요한 지점(A/B 선택지)을 먼저 목록으로 제시하고 합의 후 코드 작성. 사용자가 나중에 "이건 왜 이렇게 했어"를 묻지 않게 할 것
2. **선제적 제안**: 20년차 개발자 시각에서 사용자가 놓칠 수 있는 문제(보안·엣지케이스·유지보수)를 먼저 짚을 것
3. **확정 즉시 반영**: 대화에서 결정이 확정되면 이 CLAUDE.md의 해당 조항을 같은 커밋에서 갱신
4. 응답은 한국어로

### Git 규칙
5. **main 직접 푸시 금지**: kzion.net은 교회가 실제 사용 중인 사이트. 모든 작업은 feature 브랜치에서 하고, 사용자 확인 후 머지
6. 기존 사이트의 페이지·배포 설정을 변경하지 말 것. 예배 허브는 별도 경로 `/ppt/`로만 추가
7. 커밋 메시지는 한국어, 변경 요약 1줄 + 관련 지침 조항 번호(예: "검수 화면 3탭 구현 (지침 10번)")

### 보안 규칙 (지침 5번과 연동)
8. **API 키·시크릿을 코드나 이 문서에 절대 쓰지 말 것**. `.env`는 `.gitignore`에 포함 확인 후 작업 시작. 키는 Supabase Edge Function secrets에만 존재
9. **이 repo가 public이면 소스가 전부 공개된다는 전제로 작업**: PIN 검증·권한 확인은 클라이언트 JS가 아니라 반드시 Supabase(RLS 정책 또는 Edge Function)에서 수행. 클라이언트 코드에 PIN 값·검증 로직을 두지 말 것
10. 실제 PIN 값, 담당자 개인 이메일 등 민감값은 코드·문서·커밋 메시지 어디에도 기록 금지 (DB에만)

## 구현 확정 결정 (2026-07-02, 구현 착수 시 합의)

D1. **경로**: `/ppt/` (기존 공개 페이지 worship.html과 충돌 회피). 사이트 메뉴·푸터에 링크 비노출, robots.txt `Disallow: /ppt/` + 페이지 `noindex,nofollow`. 담당자에게는 URL 직접 공유
D2. **기술 방식**: 기존 사이트와 동일한 순수 HTML/CSS/JS, 빌드 도구 없음. 라이브러리는 CDN(PptxGenJS 등)
D3. **PIN 세션**: 기기당 30일 유지(localStorage) + 로그아웃 버튼 상시 노출. 실보안은 서버 검증(Edge Function이 PIN 검증 후 토큰 발급)이 담당 — 클라이언트 저장은 편의 기능일 뿐
D4. **주차 문서 생성**: 예배 다음 날인 월요일 0시(시애틀 PT)에 pg_cron이 다음 주일 문서 자동 생성. 문서 키는 예배 날짜(일요일, PT 기준)
D5. **순서 지정 데이터 모델**: 슬라이드 복제 저장이 아닌 **블록 ID 참조 배열** 방식(`[절1, 후렴, 절2, 후렴]`). 블록 수정 시 반복 등장분에 자동 반영. 2줄 분할 정보는 블록 내부에 저장
D6. **템플릿은 스냅샷**: 매주 문서 생성 시점에 템플릿(순서표·문구·디자인 토큰)을 문서 안으로 복사. 관리자의 템플릿 수정은 차주 생성분부터 반영(지침 16번 구현 방식)
D7. **AI 추출 응답 스키마에 `version` 필드 포함**: 프롬프트·UI·저장 데이터의 계약. 스키마 변경 시 버전 올리고 하위 호환 처리
D8. **개발 순서**: 프론트엔드를 목(mock) 데이터로 먼저 구축(1~2단계) → Supabase 연결(3단계) → Claude API(4단계) → PPT 생성·승인(5단계) → 마무리(6단계). 현재 목 단계의 PIN(1111 등)은 개발용 가짜 값이며 3단계에서 서버 검증으로 교체
D9. **크로마 밴드형 소캡션 폐기**(2026-07-02): 좌하단 곡명·절 캡션은 슬라이드에 표시하지 않는다 — 밴드에는 가사만. 곡명·절 정보는 편집·검수 화면에서 관리용으로만 사용
D10. **설교 제목 슬라이드에 설교자 표기 폐기**(2026-07-02): 순서표 14번은 설교 제목 + 본문 구절만 표시
D11. **기본 줄 길이 기준**(2026-07-02): 추출·분할 제안 시 한 줄 = 짧은 소절 2개를 병합한 길이(예: "아버지 사랑 내가 노래해 아버지 은혜 내가 노래해", 공백 포함 약 20~28자)를 기본값으로 한다. 4단계 추출 프롬프트에 명시. 더 짧게/길게는 검수 화면의 나누기(엔터)·합치기(⌫)로 조정
D12. **검수 화면 2탭 통합**(2026-07-02): 가사와 슬라이드 미리보기를 한 화면(탭)으로 — 블록 카드마다 가사 줄 아래 해당 블록의 슬라이드 미리보기를 가로 스크롤로 표시. 탭은 "가사 · 미리보기 / 원본" 2개 (지침 10번 갱신)
D13. **기도 슬라이드 표기**(2026-07-02): "기도 : 홍길동 집사" — 한 줄, 순서명과 이름 동일 크기 (순서표 8번)
D14. **서버 아키텍처**(2026-07-02): 모든 DB·스토리지 접근은 Edge Function `api` 하나를 경유(내부에서 service_role 사용). 테이블 RLS는 전부 켜고 정책 0개 = 클라이언트 직접 접근 전면 차단. PIN 검증은 DB 함수 verify_pin(bcrypt)로, 로그인 성공 시 sessions 테이블에 30일 토큰 발급(D3). 함수는 자체 토큰 인증을 쓰므로 JWT 검증 끔. 주차 문서는 getWeek 호출 시 없으면 생성(온디맨드) — 월요일 pg_cron 생성(D4)은 보조. 프로젝트: kwezbhanfxludoafmmem.supabase.co
D15. **곡당 악보 여러 페이지**(2026-07-02): 한 곡에 악보 사진 여러 장 일괄 업로드 가능. 업로드 후 곡 카드에서 페이지 순서를 **드래그로 변경**(폰: 꾹 눌러 끌기 / 데스크톱: 바로 끌기)·삭제·추가 가능 — 페이지 순서는 가사 추출(4단계)·악보 슬라이드 순서의 기준
D16. **가사 추출 모델**(2026-07-02): 기본 `claude-haiku-4-5`(비용 최저, 지침 6번). Edge Function이 DB `settings` 테이블의 `extract_model` 값을 읽으므로 관리자 화면에서 차후 변경 가능. 추출은 `api` Edge Function의 `extract`(이미지)·`extractText`(붙여넣기 우회로, 지침 12-7) 동작이 담당하고, 프롬프트에 12-1~12-6 규칙을 담아 output_config.format(json_schema)로 구조화 출력. crop 판정(12-5)은 blocks jsonb에 함께 저장. ANTHROPIC_API_KEY는 Edge Function secret으로만 존재(지침 5번)
D17. **부르는 순서를 검수 화면에 통합**(2026-07-03): 별도 순서 지정 화면 폐기. 검수 화면 하단 고정 바에서 블록 버튼(+1절, +후렴…)을 부르는 순서대로 탭 → 순서 칩이 쌓이고 즉시 자동 저장(칩 탭 = 빼기). 순서가 1개 이상이면 곡 상태 '완료', 비면 '검수 필요'로 자동 전환. "완료" 버튼 없음

## 순서·PPT 생성 재확정 (2026-07-03, 6/21 실사용 PPT 44장 실측 기반)

> 6/21 `20260621.pptx`(44장)를 실측해 순서표(조항 21)를 전면 갱신하고, 5단계 PPT 생성의 계약을 아래 D18~D26으로 확정. 조항 21 표가 이 결정들을 반영한 최신본이다.

D18. **PPTX 병합 폐기 → 외부 콘텐츠는 전부 텍스트 슬라이드 or 이미지(jpg/png)**: 사용자가 만든 기존 .pptx 슬라이드를 최종본에 끼워넣는 방식은 하지 않는다. 찬송가·봉헌송·폐회송·특송 등 "밖에서 만들어진 슬라이드"는 (1) 시스템이 텍스트로 새로 그리거나 (2) 이미지(jpg/png)로 삽입한다. → 이로써 **락 2번(브라우저 PptxGenJS 직접 생성)을 그대로 유지**(서버 변환 워커 불필요)
D19. **찬송가(예배 중 부르는 찬송)를 목사님 섹션의 '가사 텍스트 붙여넣기'로 이동**: 기존의 "예배 중 찬송가 악보 이미지 업로드"(구 순서표 7번) 폐기. 목사님이 가사를 붙여넣으면 `extractText` 엔진으로 절/후렴 분류·2줄 분할 → 크로마 밴드 가사 슬라이드. 목사님이 원하면 이미지(jpg/png)로 대체 업로드도 허용
D20. **빈 그린스크린 = 라이브/전환 고정 슬롯**: 순수 그린(#70AD47 전면, 글씨 없음) 슬라이드는 방송에서 목사님·현장을 라이브로 보여주는(크로마 키잉) 자리다. 찬양팀·성가대 곡 앞뒤에 삽입되며 임의로 없애지 않는다(관리자가 드래그로만 조정)
D21. **날짜 썸네일 자동 매핑**: 첫 장 썸네일 파일명 "시애틀시온장로교회 - N"의 N = 그 해의 N번째 주일(예: 2026년 N=27→7/5, 28→7/12 … 52→12/27). 예배 날짜로 "올해 몇 번째 주일"을 계산해 해당 번호 파일을 자동 선택. 관리자는 27~52 파일을 스토리지에 **일괄 업로드**만 하면 됨. 마지막 장("예배를 마쳤습니다")은 항상 맨 끝 고정
D22. **봉헌송·폐회송 = 고정 이미지(jpg/png) 세트**: 각각 봉헌 다음 / 축도 직전에 고정 삽입. 관리자·목사님이 이미지로 교체
D23. **설교 제목 슬라이드에서 설교자 이름 제거 재확인**(D10 유지): 제목만 표시(6/21 원본엔 "이영래 목사"가 있었으나 폐기)
D24. **성경 본문 2종**: 긴 본문=다크 전체화면형(한 장에 안 들어가면 자동으로 슬라이드 추가 생성), 짧은 구절=크로마 밴드형. 목사님 섹션의 "성경 본문(다크)" / "함께 읽는 구절(밴드)" 두 입력칸으로 구분
D25. **순서 편집 2단**: (1) 템플릿=매주 기본 순서(D6 스냅샷) (2) 각 주 문서에서 그 주만 예외적으로 드래그 추가·삭제·이동. 템플릿을 안 건드리고 특정 주만 특송·추가 순서 삽입 가능
D26. **특송 = 성가대 옵션(필요할 때만)**: 어쩌다 한 번이므로 상시 노출이 아닌 옵션. 성가대 섹션에서 추가하며 산출물은 정규 성가대 곡과 동일(곡명 그린 자막 + 가사 크로마 밴드)
D27. **사도신경 = 텍스트(다크 전체화면형) 확정**: 6/21 원본은 이미지였으나 앞으로는 고정 텍스트로 생성

## 리더용 세트 찬양 순서·데이터 정책 확정 (2026-07-05, 실사용 리더 카톡 공지 실측 기반)

> 찬양팀 리더의 실제 순서 공지(예: `V1+V2+C+(간주) / V3+C(x2) / V4+C(x4) 3번째 목소리만 / +불쌍히 여겨 구원해`)를 실측해, "부르는 순서"를 곡별 블록 클릭(D17)에서 **세트 전체 편곡 + 카톡 공지 생성 도구**로 격상. 시안(v4) 이미지로 사용자 합의 완료.

D28. **찬양팀 공간 = 리더 전용**. 신규 **세트 찬양 순서 화면**이 그 주 전체 곡의 편곡을 한눈에 담고, **이 화면이 최종 PPT 페이지 순서의 유일한 원천**이다. 곡을 ⠿ 드래그로 옮기면 PPT 페이지 순서도 자동 반영
D29. **순서 단위 = 블록 + 반복횟수(×N) + 회차(pass)**: 사람들은 "C를 4번 탭"이 아니라 "C ×4"로 사고 → 블록 칩에 ×N 스테퍼. 회차(줄) 단위로 배열(1회차 V1+V2+C / 2회차 V3+C×2 …). **×N 렌더 규칙**: (a) **1페이지 블록** → 자막 마지막 줄 뒤에 "(×N)" 표기, 한 장 띄워두고 반복(되돌아갈 일 없음) (b) **여러 페이지 블록** → **페이지를 실제로 복제해 앞으로 나열**(방송은 앞으로만 넘김, 되돌아가기 금지). 상단에 총 페이지 수 표시
D30. **특수 항목 2종**: **간주**(가사 없는 마커, 담기 팔레트의 `+간주` 버튼 — PPT엔 슬라이드 없이 넘김/홀드) / **✎메모**(팀 지시 통합 = 다이내믹 큐·즉흥 멘트·키·크로스곡 연결 — **자막(PPT)엔 안 뜨고 카톡 텍스트에만 나옴**). ~~태그 칩~~ 개념 폐기(즉흥 멘트도 ✎메모로 통합; 정말 자막에 띄울 구절은 곡 가사에 줄 추가로 처리)
D31. **이중 출력**: 한 소스에서 (1) **PPT 자막 페이지**(블록·×N·간주만 반영) + (2) **카톡용 순서 텍스트**(리더 표기 그대로 자동 생성, 편집 가능 미리보기 + 전체 복사 원탭). 리더가 카톡에 손타이핑하던 공지를 대체. **표기 표준 = V1/V2/C/PreC**(내부 추출 라벨 1절↔V1, 후렴↔C 매핑)
D32. **D17 진화**: 부르는 순서를 검수 화면 하단 바에서 **세트 화면으로 이동**. 곡별 검수 화면 = **가사 확인·오독 교정·슬라이드 나눔 전용**, 편곡은 세트 화면에서. 곡 가사 수정은 세트 화면에서 해당 곡으로 드릴인
D33. **가사 붙여넣기 우선 + 이미지(JPG·PDF) + 해상도 스펙**: 곡 추가 시 **📋 가사 붙여넣기를 추천·기본**("가사 텍스트 있으면 붙여넣기가 제일 정확")으로 유도, 🖼 악보 이미지는 보조. 붙여넣기 블록화 = 빈 줄·머리표 기준 자동 나눔 + AI 자동 라벨(반복=후렴/C, 순서=V1/V2) + 라벨 탭 편집·나누기/합치기로 확인. **이미지 해상도 안내**: 권장 긴 변 2000px+(200~240DPI) / 최소 1200px(미만이면 자동 경고) / 상한 2560px(넘어도 무의미 — Sonnet5 2576px·3.75MP 캡) / 무엇보다 또렷·고대비. **PDF 지원**: pdf.js로 200~240DPI 렌더 후 동일 추출 파이프라인 합류
D34. **악보 이미지 주 단위 자동 정리(개인정보·저작권·용량)**: **매주 일요일 23:00(시애틀 PT)** pg_cron으로 `scores` 버킷의 지난 주차 악보 이미지 삭제(예배 후). 텍스트로 넣은 곡은 애초에 이미지 미저장. 이미지는 비공개 버킷 + 시간제한 서명 URL로만 열람(공개 노출 없음, D14), Anthropic API는 학습에 미사용. 구현: Edge Function `api` 정리 액션 + pg_cron(UTC·DST 변환 주의, D4)
D35. **추출 정확도 개선 확정(2026-07-04, 라이브)**: 클라이언트 리사이즈 1920→2560px·JPEG 0.85→0.92(Sonnet5 고해상도 비전 활용), `extract` `max_tokens` 10000→24000 + `stop_reason:max_tokens` 잘림 처리, 추출 규칙 5 재작성=**안 보이면 지어내지 말고 low로 표시**(환각 방지, 곡 제목·관용구로 빈칸 채우기 금지). 지침 12-4·12-5와 연동

D36. **고정 순서 추가 + 그린스크린 보강 + 성경 본문 폰트 확정(2026-07-12, 라이브)**: (a) **합심기도 뒤 "다음 세대를 향한 축복" 고정 순서** 신설 — 제목=그린 자막형 1장 + 가사=크로마 밴드형(2줄씩). 가사는 **관리자 문구 관리**에서 편집(settings `blessing_lyrics`, 빈 줄=슬라이드 구분). 앞뒤에 빈 그린스크린(라이브) 삽입. template.js `blessing` source + generate.js expandSlot. (b) **짧은 구절(함께 읽는 구절, #19) 앞에도 빈 그린스크린** 추가(reading-live). (c) **성경 본문 카드(긴/짧은, addVerseCard)** = 흰 카드+네이비 글자+네이비 구절칩 유지, 본문·칩 **33pt 고정·Pretendard Black·줄간격 1.3**(ppt.css .vcard-*·preview.js PASSAGE_CHARS 400→230·generate.js). (d) **특송 이름 미반영 회귀 수정**: generate.js가 getWeek 원본행(song_type/song_performer)을 읽도록 getSongType/getPerformer 헬퍼 추가

D37. **지난 곡 불러오기(2026-07-12)**: 찬양팀이 곡 제목 입력 후 칸을 벗어나면(blur), 같은 곡 과거 기록이 있으면 **가장 최근 1건**의 콘티(arrange·ord)+자막(blocks)을 자동 채움 제안(확인 후 복제, 비파괴). **곡 마스터 승격 안 함 = 주차에 묶고 복제**. 버전 목록·검색창 없음(목적=반복 타이핑 회피). 잘못 매칭 방지로 첫 줄 표시. 서버 `songLookup` 액션(정규화=공백·기호 무시, 이번 주 제외, 내용 있는 것만). 아직 가사 없는 곡에만 제안(기존 작업 보호). 성가대 확장은 차후

D38. **그린스크린(라이브 슬롯) 보강 + 찬송가 절 번호 정리(2026-07-19, 실사용 피드백)**: (a) **함께 읽는 짧은 구절 — 각 구절 앞마다** 빈 그린스크린 자동 삽입(구절 단위 1장 — 한 구절이 카드 여러 장으로 나뉘어도 그린은 앞에 1장, 구절 없으면 생략. reading_short 확장에서 동적 생성 — 고정 슬롯 아님) (b) **기도(#10) 앞뒤** 그린스크린(prayer-live-1/2) (c) **교회 소식(#17) 뒤** 그린스크린(news-live) — 조항 21 표에 준용 (d) **찬송가 가사 절 번호 표기 제거**: "(1) 가사"·"[1] 가사"·"1. 가사"·"1)" 등을 붙여넣어도 숫자는 자막에 미노출, 절 경계·라벨(1절·2절)로만 활용(pastor.js stripHymnVerseNums → pasteToBlocks)

---

# 교회 주일예배 PPT 자동 생성 허브 — 프로젝트 지침 (v3.2)

> v3 → v3.2 변경: 실전 악보 테스트 결과(12-1~12-5) + 흐릿한 인쇄 대응(12-6~12-7, 반자동 붙여넣기 포함) 병합, 검수 화면 3탭 구조(10번), 체크리스트 실측 기록. 이 문서 하나가 최신 — v3·v3.1 변경분은 폐기.

## 프로젝트 목적
kzion.net(교회 웹사이트, GitHub 배포 정적 사이트)에 비공개 페이지를 추가해, 세 담당자(목사님·찬양팀·성가대)가 최소 입력만 하면 예배용 .pptx 한 파일이 자동 생성되는 시스템을 구축한다. 반복 타이핑 업무 제거가 목표. 관리자(본부장)가 최종 검토·승인·다운로드한다.

## 사용자와 역할 (PIN 4개, 역할별)
- **목사님**: 설교 제목·본문, 기도 순서(담당자명), 찬송가 선택, 설교 중 성경 본문(수동 붙여넣기), 매주 다른 악보 페이지(예배 중 찬송가) 업로드, 고정 악보 교체 권한
- **찬양팀**: 악보 이미지 업로드 → 가사 자동 추출 → 검수 → 부르는 순서 지정
- **성가대**: 찬양팀과 동일 워크플로우 + 곡명 입력
- **관리자(본부장)**: 전체 열람, 최종 검토·반려·승인, PPT 생성·다운로드, 슬라이드 템플릿 관리, 역할·이메일 관리
- 각 역할은 자기 섹션만 수정 가능. 담당자 이메일은 DB 역할 테이블(역할–PIN–이메일)로 관리, 화면에서 수정 가능

## 컨셉 락 (확정 사항 — 변경 시 이 문서를 즉시 갱신할 것)

### 플랫폼·스택
1. kzion.net에 페이지 추가. 구글 폼 방식 기각(드래그앤드랍·블록 순서 UX 불가)
2. PPT 생성: PptxGenJS로 브라우저에서 직접 생성. 다운로드는 데스크톱·모바일 모두 지원 + 모바일용 "이메일로 pptx 받기" 버튼(Resend 재사용)
3. 저장/협업: Supabase 무료 티어. 매주 예배 문서 자동 생성, 역할별 섹션 입력, 실시간 저장. 검수·수정 내용은 자동 저장(저장 버튼 누락 사고 방지)
4. 이메일: Resend(무료 월 3,000통) / 스케줄러: Supabase pg_cron(UTC 기준 — 시애틀 PT 서머타임 변환 처리 필수)
5. API 키 보안: 정적 사이트에 키 노출 금지 → Supabase Edge Function secret으로만 저장, 서버 측 프록시 호출
6. Claude API 계정: 교회 공용 이메일로 발급, $5 선불 충전, 월 사용 한도 설정. 예상 비용 월 $1~2

### 모바일 완전 지원 (전 과정 폰에서 100% 가능, 데스크톱 전용 기능 금지)
7. 업로드: 데스크톱 드래그앤드랍 / 모바일 탭 → 카메라 촬영·사진첩·파일 선택
8. 업로드 직후 밝기·기울기 간이 체크 → 재촬영 안내(강제 아님). 추출 신뢰도 낮으면 검수 화면 경고 배지
9. 이미지 자동 리사이즈: 클라이언트에서 가로 1920px, JPEG 압축
10. 검수 화면 모바일: **2탭 구조 — 가사·슬라이드 미리보기 통합 / 원본 이미지(핀치 줌)** (2026-07-02 갱신, D12 — 원래 3탭에서 가사·미리보기 탭 통합: 블록 카드 안에 가사 줄 + 해당 블록 슬라이드를 함께 표시). 저신뢰 하이라이트 단어를 원본과 눈으로 대조 가능. 크롭 도구는 터치(핀치 줌) 대응
11. 테스트 1순위: 아이폰 사파리, 2순위 안드로이드 크롬 — 촬영→업로드→추출→검수→순서지정 풀 사이클

### 악보 워크플로우
12. 악보 이미지 → Claude API 비전이 가사 추출 + verse/chorus/bridge 블록 분류 → 카드 표시 → 검수(필수, AI 정확도 90~95%, 나머지는 사람이 잡음) → 순서 클릭 → 슬라이드 생성

12-1. **가사 정규화 규칙**(추출 프롬프트에 명시): 멜리스마 대시 제거("드-리니"→"드리니"), 음표 단위로 붙은 음절의 띄어쓰기 복원("아버지사랑내가노래해"→"아버지 사랑 내가 노래해")

12-2. **다절 스택 분리**: 찬송가류는 악보 한 줄 아래 2~3절 가사가 세로로 쌓임 → AI가 행 단위가 아닌 **절 단위로 재조립**하여 절별 블록 카드로 출력

12-3. **도돌이표·1/2절 엔딩 재조립**: 1st/2nd ending이 있는 곡은 악보 기호 기준으로 가사 순서 복원(절→해당 엔딩→후렴). 후렴 공유 곡은 절 N개 + 후렴 1블록으로 분리 → 순서 지정 화면에서 절1→후렴→절2→후렴 클릭(락 15번 복제 방식과 결합)

12-4. **저신뢰 단어 하이라이트**: 추출 시 확신 낮은 단어에 표시 → 검수 화면 노란 하이라이트. 검수를 "전체 정독"에서 "표시 지점 확인"으로 단축. 실측 검증됨: 표시 3곳 중 1곳이 실제 오류("뭉쳐 있고"), 사람이 잡아야 할 지점을 정확히 짚음

12-5. **사진 잘림 감지 — 시각적 근거로만 판정**: 오선이 이미지 가장자리에서 물리적으로 절단됐거나 종지선(겹세로줄)이 없을 때만 표시. 가사·멜로디 내용으로 추측 금지(실측 오탐 1건). 배지는 질문형("다음 페이지가 있나요?") + 원클릭 해제

12-6. **흐릿한 인쇄 감지(사후 판정)**: 성가대 인쇄물 등 원본 자체가 흐린 경우는 8번(촬영 조건 체크)으로 못 잡음 → 저신뢰 단어 비율이 기준(예: 10%) 초과 시 "원본 인쇄가 흐린 것 같아요 — 검수를 꼼꼼히" 배지 + 10번 원본 탭으로 대조 유도

12-7. **수동 입력 우회로**: 추출이 못 쓸 수준이면 "가사 직접 입력" 버튼 → 타이핑 또는 **가사 붙여넣기(반자동 모드: 붙여넣은 텍스트를 AI가 절/후렴 분류 + 2줄 분할 제안 — 같은 API를 텍스트로 호출)** → 이후 검수·순서 지정은 동일 플로우 합류. 추출 실패가 시스템 전체를 막지 않게 하는 안전장치

13. 슬라이드 블록 5유형: 텍스트(설교·기도·성경) / 가사(AI 추출) / 악보 원본(이미지 통짜, 업로드 시 크롭 도구 옵션) / 고정(매주 자동 삽입, 교체는 목사님 PIN) / 이미지(타이틀·엔딩, 교체는 관리자)
14. 악보 원본 표시: 기본 통짜 1장(16:9 전체 채움, 4:3 원본은 흰 배경 중앙 배치). 실제 프로젝터 테스트 후 최종 확정
15. 후렴 반복: 슬라이드 복제 방식(순서 지정 UI에서 같은 블록 여러 번 클릭 = 자동 복제, 방송은 앞으로만 넘김)

### 디자인 시스템 (2026-07-02 확정 — 샘플: 주일예배_디자인시스템_v3.pptx)
16. **디자인 토큰**(DB template 테이블로 이관, 관리자 화면에서 수정 → 차주 생성분부터 반영)
    - 크로마키 그린 `#70AD47` (기존 PPT 슬라이드 5·20·33 전면색에서 추출, OBS 사용자 지정 키잉)
    - 하단 밴드 검정 `#000000` / 다크 전체화면 배경 `#14181F` / 본문 웜화이트 `#F5F2EA` / 흰색 `#FFFFFF` / 악센트 골드 `#C9A66B`(절 번호·캡션 전용) / 밴드 캡션 `#6E6E6E`
    - 폰트: **Pretendard, 전 텍스트 Bold** (방송 컴퓨터에 본부장이 설치)
17. **레이아웃 5종**
    - **그린 자막형**: 전면 그린 + 흰 볼드 글씨(화면 하단부 배치, 얇은 검정 그림자) — 순서명·설교 제목. 배경·밴드 없음, 영상 위 자막으로 키잉
    - **크로마 밴드형**: 상단 80% 그린 + 하단 20% 검정 밴드, 흰 볼드 가사 2줄 가운데 정렬 — 찬양팀/성가대 가사, 함께 읽는 단문 구절. ~~좌하단 소캡션(곡명·절)~~ → 폐기(2026-07-02, 사용자 확정 — 밴드에는 가사만)
    - **다크 전체화면형**: `#14181F` 배경, 상단 골드 캡션, 웜화이트 볼드 본문 — 사도신경, 설교 중 성경 장문(절 번호 골드)
    - **악보 통짜형**: 원본 이미지 그대로 전체 채움
    - **이미지형**: 기제작 이미지 삽입 — 타이틀(12월분까지 제작 완료), 엔딩(예배를 마쳤습니다)
18. 가사 분할: 슬라이드당 2줄 고정, 하단 배치. AI가 2줄씩 제안 → 검수 화면에서 조정
19. 검수 화면 동작: 줄 사이 분할선 클릭 토글(드래그 불필요, 모바일 호환) / 줄 내 커서+엔터로 줄 분리 / 넘침 자동 경고(노란 표시) / 오타 수정 같은 화면에서 / 분할선은 블록(verse/chorus) 경계를 넘지 못하게 잠금
20. 초록 계열 색상 사용 금지(글씨·요소 전체) — 크로마키 키잉 뚫림 방지, 템플릿 차원 잠금. 골드가 유일한 유채색 악센트

### 예배 순서 기본값 (2026-07-03 갱신 — 6/21 실사용 PPT 44장 실측 기반, D18~D27)
21. 기본 순서표(23슬롯). 빈 그린스크린(라이브 슬롯, D20)·앞뒤 배치까지 실측 반영. 각 슬롯은 관리자가 드래그로 추가·삭제·이동(D25). 설교 제목은 성경 본문 앞 1회만.

| # | 문구/내용 | 유형 | 레이아웃 | 입력자 |
|---|---|---|---|---|
| 1 | 날짜 썸네일(N번째 주일 자동 선택, D21) | 이미지 | 이미지형 | 관리자(일괄 업로드) |
| 2 | 예배의 부름 | 고정 | 그린 자막형 | — |
| 3 | 사도신경 (텍스트, D27) | 고정 | 다크 전체화면형 | — |
| 4 | 다함께 찬양 + 팀명 | 고정 | 그린 자막형 | — |
| 5 | 빈 그린스크린 (라이브, D20) | 고정 | 그린스크린 | — |
| 6 | 찬양팀 곡 가사 | 가사 | 크로마 밴드형 | 찬양팀 |
| 7 | 빈 그린스크린 (곡 뒤 라이브, D20) | 고정 | 그린스크린 | — |
| 8 | 합심기도 | 고정 | 그린 자막형 | — |
| 8-1 | 빈 그린스크린 (라이브, D36) | 고정 | 그린스크린 | — |
| 8-2 | 다음 세대를 향한 축복 — 제목 + 가사(밴드 2줄씩) (D36) | 고정 | 그린 자막형 + 크로마 밴드형 | 관리자(문구 관리) |
| 8-3 | 빈 그린스크린 (라이브, D36) | 고정 | 그린스크린 | — |
| 9 | 찬송가 가사 (텍스트 붙여넣기, 이미지 대체 가능, D19) | 가사 | 크로마 밴드형 | 목사님 |
| 10 | 기도 + 담당자명 | 고정 + 입력(이름) | 그린 자막형 | 목사님 |
| 11 | 성가대 곡명 + "시온 성가대" | 입력(곡명) + 고정 | 그린 자막형 | 성가대 |
| 12 | 성가대 가사 | 가사 | 크로마 밴드형 | 성가대 |
| 13 | 빈 그린스크린 (곡 뒤 라이브, D20) | 고정 | 그린스크린 | — |
| 14 | 봉헌 | 고정 | 그린 자막형 | — |
| 15 | 봉헌송 악보 3장(나의 생명 드리니) | 고정 이미지 | 악보 통짜형(4:3 중앙) | 관리자·목사님 교체 |
| 16 | 빈 그린스크린 (라이브, D20) | 고정 | 그린스크린 | — |
| 17 | 교회 소식 | 고정 | 그린 자막형 | — |
| 18 | 설교 제목 (설교자 표기 폐기, D23) | 입력 | 그린 자막형 | 목사님 |
| 18-1 | 성경 본문 앞 긴 본문(자동 분할) | 입력 | 다크→흰카드 (addVerseCard) | 목사님 |
| 18-2 | 빈 그린스크린 (짧은 구절 앞 라이브, D36) | 고정 | 그린스크린 | — |
| 19 | 함께 읽는 짧은 구절 (D24) | 입력 | 크로마 밴드(흰카드) | 목사님 |
| 20 | 빈 그린스크린 (라이브, D20) | 고정 | 그린스크린 | — |
| 21 | 폐회송 악보 10장(절 라벨 포함) | 고정 이미지 | 악보 통짜형 | 관리자·목사님 교체 |
| 22 | 축도 | 고정 | 그린 자막형 | — |
| 23 | 예배를 마쳤습니다 (항상 맨 끝) | 고정 이미지 | 이미지형 | 관리자 교체권 |

- **특송(옵션, D26)**: 성가대가 필요할 때만 추가. 곡명 그린 자막 + 가사 크로마 밴드(정규 성가대 곡과 동일 산출물). 상시 노출 아님
- 봉헌송·폐회송 고정 세트는 6/21 원본 기준 초기 등록. 곡이 바뀌면 관리자·목사님이 이미지(jpg/png)로 교체(D22)
22. 슬라이드 템플릿은 하드코딩 금지, DB 데이터로 분리({순서, 유형, 문구, 레이아웃, 소속 섹션}). 관리자 화면에서 문구 수정·순서 드래그·페이지 추가/삭제 가능
23. 초기 문구는 위 표(기존 PPT 순서) 그대로, 이후 화면에서 자유 수정

### 최종 생성·검토 흐름
24. 3섹션 완료 → 관리자 자동 이메일(해당 주 문서 바로가기 링크 포함) → 화면에서 슬라이드 미리보기로 검토(pptx 왕복 제거) → 승인 시 생성·다운로드
25. 반려 기능(1차 포함): 섹션별 코멘트 + 반려 → 담당자 자동 이메일 → 수정 완료 시 관리자 재알림. 시스템 밖 커뮤니케이션 방지
26. 생성 이력(누가·언제) 기록

### 오류·건의 접수
27. 모든 화면에 상시 신고 버튼 → 유형(오류/건의) + 설명 + 스크린샷(선택 항목) → 시스템 정보 자동 첨부(역할, 화면, 시각, 브라우저/기기, 문서 상태, JS 에러 로그) → DB 저장 + 관리자 이메일(제목에 [오류]/[건의])

### 성경 본문
28. API/DB 내장 없이 목사님 수동 붙여넣기. 입력창 극단적으로 단순하게 — 매주 입력 항목은 사실상 5개: 설교 제목 / 본문 구절 표기 / 성경 본문 붙여넣기 / 함께 읽는 구절 / 기도 담당자명 + 찬송가 악보 업로드 1건

## 미결 사항 (다음 대화에서 결정)
1. 리마인더 정책 — 보류 중. 후보: A) 토 19:00 1회 / B) +21:00 2차(추천) / C) B+완료 알림. pg_cron+Resend 인프라 완성 후 켜기만 하면 됨
2. 알림 채널 — 1차 이메일. 실운영 1개월 후 "이메일 못 봤다" 사고 발생 시 카톡/SMS 재논의

## 프로토타입 검증 체크리스트
- ~~실사용 악보 추출 정확도 테스트~~ → **완료(2026-07-02)**: 방송용 편집 이미지 5장(난이도 하) 100% + 실전 악보 사진 3장(CCM 2·찬송가 4부 합창보 1) 단어 정확도 약 95%. 저신뢰 표시 3곳 중 실오류 1곳(하이라이트 유효성 확인), 잘림 감지 오탐 1건(→ 12-5 판정 기준 수정). 락 12번의 "90~95% + 검수" 가정 실측 확인
- 생성 pptx를 실제 방송 컴퓨터에서 크로마키 키잉 테스트 — 특히 `#70AD47` 키잉 마진 확인, 뚫림 발생 시 표준 크로마그린 `#00B140` 전환 검토
- 그린 자막형 흰 글씨가 밝은 영상 위에서 묻히는지 확인(그림자 강도 조정 여지)
- 실제 프로젝터/화면에서 악보 통짜 슬라이드·다크 성경 본문 가독성 확인
- 방송 컴퓨터 Pretendard 설치 후 생성 pptx 폰트 정상 표시 확인
- 흐릿한 인쇄 악보 실물로 12-6 배지 기준(저신뢰 10%) 검증
- 아이폰 사파리 풀 사이클 테스트

## 리스크 메모
- 저작권: 찬송가 가사 DB 내장 회피(목사님 수동 입력·업로드 방식 유지). CCM 가사는 교회 CCLI 라이선스 범위 내 사용
